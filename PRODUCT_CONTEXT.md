# 注意： 此產品背景應隨著每次得到主管回饋時更新，以防AI過度解讀或過度設計

## 產品演進脈絡（影響範疇判讀，2026-06-23 更新）
- 現行方向：製作一個「公版」，由主管拿去 demo、展示給潛在使用者並收集回饋。
- 「公版」定義（2026-06-23 主管確認）：公版＝demo 用的**基底版本**。未來每個客戶的產品都從這個公版**各自增減功能**而來。
  - ⚠️ 判讀重點：客製化發生在**程式碼/分支層級（per-customer fork 增減）**，**不是**一套系統同時服務多客戶的多租戶 SaaS。
  - 因此**不需要**為「功能增減」建 feature-flag 框架、外掛系統或多租戶引擎；增減是按客戶改 code，不是 runtime 配置。公版本身維持單租戶、單機假設。
- 已捨棄方向：訂閱制多租戶。code 中既有的 auth / admin / 2FA / session /
  rate-limiter 屬此舊方向的遺產。
- 處理原則：上述 legacy **凍結保留，不主動移除、也不再擴充**（能跑就留，未來方向
  若轉回訂閱仍用得到）。新功能一律照下方核心範疇做最小設計，不得沿訂閱制假設延伸。

# 產品公版核心範疇與設計限制
1. 交易與提醒機制：定義為「事件驅動的通知與條件下單系統」（2026-08-03 前為純通知）
* 功能： 支援到價提醒、限價單/市價單觸發通知、移動停損提醒、TWAP（時間加權）拆單提醒。將來可能新增更多交易方式。
* ⚠️ 給 AI 的架構限制（防 Overdesign）：
    * 通知＋下單雙能力，分三階段演進（2026-08-03 組長方向，取代原「公版只通知」限制）： 之後會取得富邦交易帳號，功能按階段推進：
      - **階段一（現況）＝僅通知**：條件滿足時發送通知，不下單。
      - **階段二＝通知＋確認下單**：觸發時通知使用者，使用者**確認後**才送出委託（需要「待確認」狀態與逾時處理）。
      - **階段三＝通知＋直接下單**：使用者**建單時即授權**「觸發即下單」，到價直接送委託不再確認。
      - 兩種下單模式都屬使用者明確授權的動作（一個授權在觸發後、一個授權在建單時），皆不算「系統自動決策」（第 4 點紅線不變）。⚠️ 判讀重點：**做當前階段就好**，不要為未來階段預埋欄位或狀態機；階段三到來時是否保留階段二的確認模式（變成每張單可選），屆時再定。
    * 新增交易方式＝列舉式擴充，非框架： 「保持彈性」僅指維持現有 Pydantic discriminated union + DB 列舉的做法（加一種就改一處列舉）。**不需要、也不要建立動態策略註冊（registry）、外掛系統或策略工廠。**
    * 通知與執行解耦、但不預先抽象： 程式上把「判斷觸發」與「執行動作」切開，不要把下單邏輯寫死在通知流程裡。第一個下單實作直接寫具體的券商 client；**不要先建 OrderExecutor interface / ABC**，等出現第二家券商實作再抽象。
2. 互動管道：Web 與 Telegram 群組
* 現況：web 仍是完整管理介面；Telegram Bot 已串接為單一設定群組的 demo 輸入管道，可接收文字、逐步補充交易意圖，並在原 bot 訊息上完成確認或取消。
* Telegram inbound workflow 目前只支援限價買進、限價賣出、市價買進、市價賣出四種通知型意圖。群組以 `TELEGRAM_CHAT_ID` allowlist 限制，owner 由 `TELEGRAM_OWNER_EMAIL`（預設 `admin@tingfong.com`）解析；確認後只建立 `trade_intent_core`，不送出券商委託。
* 語音輸入（語音下單、語音設定提醒）仍待後續優先序；採現成 STT，再交給同一 LLM 結構化，不自建 NLP。
* ⚠️ 給 AI 的架構限制（防 Overdesign）：
    * 用語音轉文字（STT）非自建 NLP： 語音輸入應優先使用現成的 STT API（如 Whisper 或 Telegram 自帶語音轉文字），後續透過簡單的關鍵字或 LLM 結構化文字即可。不需要設計一套複雜的意圖分析系統。
    * LLM 供應商對齊： 若用 LLM 做文字結構化，沿用本專案既定的 LLM 供應商，不要為此另外引入第二家 LLM。
3. 資料源與系統負載：極小規模的 PoC 架構
* 功能（2026-08-03 更新）： 新方向採用**富邦新一代 API（Fubon Neo，v2.2.8）**作為報價與即時資料源，並作為未來的下單通道（將取得富邦交易帳號）。原永豐 shioaji API **是否續用是未知數，不排除保留**——判讀時不要假設 shioaji 已退場，也不要再基於「只有 shioaji」設計。
* 訂閱上限的性質（依實際使用的券商而定，皆為**券商端真實外部上限**，不是我們自訂的內部假設）：
  - 富邦：行情 WebSocket **每條連線 300 檔、最多 7 條連線**；Web API 另有速率限制（日內行情/快照 300/min、歷史行情 60/min，超過回 429）。
  - 永豐 shioaji（免費方案，若續用）：**最多 5 檔訂閱**，超過券商端直接擋（對應 code 既有的 `QuoteSubscriptionLimitExceeded` 概念）。
  - ⚠️ 判讀重點：demo 實際用量是個位數到數十檔。上限一律當**無法超過的天花板**處理，到量時優雅回報「已達訂閱上限」即可；**不需要**建配額管理系統、動態擴容或多帳號輪替等機制。
* ⚠️ 給 AI 的架構限制（防 Overdesign）：
    * 不需考慮分散式巨量資料： 實際訂閱量在數十檔內、單機部署，資料庫（DB）和記憶體（Memory）的壓力極小。拒絕 AI 提議使用如 Kafka、大型 Redis 叢集或複雜的時間序列資料庫（Time-Series DB）。
    * 佇列／鎖／快取一律走 PostgreSQL： 對齊既有決策（V1 Postgres-everywhere），用 PG 的 SKIP LOCKED、row-lock、in-process cache 即可，**不另外引入獨立 Message Queue 或 Redis**。
4. 產品定位防線：人在迴圈中的盯盤／輔助決策工具，非自動化量化平台
* 定位： 「幫使用者盯盤、到價提醒、**經使用者確認後**輔助下單」。下單能力與「是不是量化平台」是兩件事——能下單不等於變成量化平台。
* 下單（2026-08-03 更新）： 下單納入功能方向。公版先接**富邦**——單純因為我們剛好會有富邦帳號可下單，**不代表富邦是唯一或最終券商**；將來實際串接哪家券商**看各客戶使用什麼券商**，屬 per-customer 客製（透過第 1 點的解耦切口換實作）。「人在迴圈」的形式＝所有委託都源自使用者**明確建立並授權**的交易意圖（含停損停利等條件單：使用者設定價位，觸發即執行）。
* 下單行為細則（2026-08-03 定案）：
  - **確認時點＝按第 1 點三階段走**：階段二為觸發後確認、階段三為建單時一次授權（觸發即下單）。實作只做當前所在階段。
  - **公版預設接券商測試環境**（不花真錢），真單環境以 config 切換（同 `SHIOAJI_SIMULATION` 樣式）。
  - **券商帳號＝每套部署一個**，放 `.env`：公版填 demo 測試帳號，客戶版換填客戶自己的帳密。**不做** per-user 綁定各自券商帳號（那是已捨棄的多租戶形狀）。
* ⚠️ 給 AI 的架構限制（防 Overdesign）— 以下為整條產品線紅線，即使某客戶版能下單也不做：
    * 不自動決策買賣： 下單一律由使用者觸發／確認，系統不自動跑單、不代客戶決定買賣。
    * 不做回測系統（Backtesting）： 拒絕設計任何歷史資料回測、策略優化或複雜的數學公式計算模組。
    * 不做高頻交易： 不追求微秒（Microsecond）級的延遲優化。
